/*
    Game of Primes
    CS 601 Programming Assignment 2
    Sankalp Gambhir
    180260032
*/

#include <iostream>
#include <vector>

bool check_strategy(std::vector<int> exponents, bool player = true);

int main(){

    // while input is being given, loop and check
    while(false){
        // take input

        // call prime checker
        std::cout << (int) check_strategy({1, 1, 1});
    }

    return 0;
}